{% extends 'base.html.twig' %}

{% block title %}Admin Voir Genre{% endblock %}

{% block body %}

    <div class="container my-5">

        <h3>Informations du genre</h3>

        {% if genre %}
        <table class="table">
            <thead>
                <tr>
                    <th scope="col">N°</th>
                    <th scope="col">Titre</th>
                    <th scope="col">E</th>
                    <th scope="col">X</th>
                </tr>
            </thead>
            <tbody>                
                <tr>      
                    <td>{{ genre.id }}</td>
                    <td>{{ genre.titre }}</td>                               
                    <td><a href="{{ path('app_modifier_genre', {id: genre.id}) }}" ><i class="bi bi-pen"></i></a></td>
                    <td><a href="{{ path('app_supprimer_genre', {id: genre.id}) }}" ><i class="bi bi-trash"></i></a></td>
                </tr> 
            </tbody>
        </table>
        {% endif %}

    </div>

{% endblock %}
